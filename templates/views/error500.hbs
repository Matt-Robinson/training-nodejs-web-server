<!DOCTYPE html>

<html>
    <head>
        <title>{{title}}</title>
        {{>icon}}
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        <div class="main-content">
            {{>header}}
            <p>{{errortext}}</p>
        </div>
        {{>footer}}
    </body>
</html>